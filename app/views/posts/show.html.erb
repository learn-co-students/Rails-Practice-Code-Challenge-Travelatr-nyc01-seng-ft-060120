<h1><%=@post.title%></h1>
<h3><%=link_to @post.blogger.name, blogger_path(@post.blogger)%></h3>
<h3><%=link_to @post.destination.name, destination_path(@post.destination)%></h3>
<p><%=@post.content%></p>

<%= button_to "Like Post", @post, method: 'like' %> Likes: <%= @post.likes %>